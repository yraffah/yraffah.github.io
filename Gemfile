source 'https://rubygems.org'

# These are required for github-pages gem
require 'json'
require 'open-uri'
versions = JSON.parse(open('https://pages.github.com/versions.json').read)

gem "jekyll-theme-cayman"
gem "jekyll-paginate"

